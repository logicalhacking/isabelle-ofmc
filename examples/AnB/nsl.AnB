Protocol: NSL	# Verified

Types: Agent A,B;
       Number NA,NB;
       Function pk

Knowledge: A: A,pk,inv(pk(A)),B;
	   B: B,pk,inv(pk(B))

Actions:
A->B: {NA,A}(pk(B))
B->A: {NA,NB,B}(pk(A))
A->B: {NB}(pk(B))

Goals:
A *->* B: NA
B *->* A: NB

